<UserControl x:Class="ToolCollectionManager.Views.SoftwareEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Width="500">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1">
            <TextBox Text="{Binding Software.Name}" materialDesign:HintAssist.Hint="软件名称" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,8"/>
            <TextBox Text="{Binding Software.Description}" materialDesign:HintAssist.Hint="描述" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,8" TextWrapping="Wrap" Height="60"/>
            <DockPanel Margin="0,8">
                <Button DockPanel.Dock="Right" Content="..." Command="{Binding SelectExecutableCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Width="40" Margin="8,0,0,0"/>
                <TextBox Text="{Binding Software.ExecutablePath}" materialDesign:HintAssist.Hint="执行文件路径" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
            </DockPanel>
            
            <DockPanel Margin="0,8">
                <Image Source="{Binding Software.IconPath}" Width="32" Height="32" DockPanel.Dock="Right" Margin="8,0,0,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                <TextBox Text="{Binding Software.IconPath}" materialDesign:HintAssist.Hint="图标路径 (自动生成)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" IsReadOnly="True"/>
            </DockPanel>
            <TextBox Text="{Binding Software.Version}" materialDesign:HintAssist.Hint="版本" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,8"/>
            <TextBox Text="{Binding Software.Developer}" materialDesign:HintAssist.Hint="开发者" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,8"/>
            
            <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" DisplayMemberPath="Name"
                      materialDesign:HintAssist.Hint="分类" Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="0,8"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
            <Button Content="取消" Command="{Binding CancelCommand}" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,8,0"/>
            <Button Content="保存" Command="{Binding SaveCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
