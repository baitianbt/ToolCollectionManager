<UserControl x:Class="ToolCollectionManager.Views.SoftwareDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Width="600" Height="500">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel LastChildFill="True" Margin="0,0,0,16">
            <materialDesign:PackIcon Kind="Application" Width="48" Height="48" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            <StackPanel Margin="16,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Software.Name}" Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                <TextBlock Text="{Binding Software.Category.Name}" Style="{StaticResource MaterialDesignCaptionTextBlock}"/>
            </StackPanel>
            <Button Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" 
                    Style="{StaticResource MaterialDesignFlatButton}" 
                    HorizontalAlignment="Right" DockPanel.Dock="Right"
                    Content="CLOSE"/>
        </DockPanel>

        <!-- Content -->
        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <TextBlock Text="Description" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" Margin="0,8"/>
                <TextBlock Text="{Binding Software.Description}" TextWrapping="Wrap" Opacity="0.8"/>

                <TextBlock Text="Details" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" Margin="0,16,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Version:" FontWeight="Bold" Margin="0,0,8,4"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Software.Version}" Margin="0,0,0,4"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Developer:" FontWeight="Bold" Margin="0,0,8,4"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Software.Developer}" Margin="0,0,0,4"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Path:" FontWeight="Bold" Margin="0,0,8,4"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Software.ExecutablePath}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Software.ExecutablePath}" Margin="0,0,0,4"/>
                </Grid>

                <TextBlock Text="Screenshots" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" Margin="0,16,0,8"/>
                <ItemsControl ItemsSource="{Binding Software.Screenshots}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Image Source="{Binding ImagePath}" Width="150" Height="100" Stretch="UniformToFill" Margin="4"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
             <Button Content="EDIT" Command="{Binding EditCommand}" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,8,0"/>
             <Button Content="CLOSE" Command="{Binding CloseCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
